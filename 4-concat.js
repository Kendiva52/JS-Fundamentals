#!/usr/bin/node

// Access the command-line arguments (skip first two elements)
const arg1 = process.argv[2];
const arg2 = process.argv[3];

// Print the output in the required format
console.log(`${arg1} is ${arg2}`);
